{% extends 'base.html.twig' %}

{% block body %}
    <main>
        <div class="container-fluid" id="img-accueil"></div>
        <div class="container">
            <div class="row align-items-center">
                <div class="col-sm text-center">
                    <h1>{{ 'myTicket'|trans }}</h1>
                </div>
                <div class="col-sm">
                    <input type="text" class="form-control" id="nbBillet" placeholder="{{ 'nbTicket'|trans }}">
                </div>
                <div class="col-sm text-center">
                    <button type="button" class="btn btn-primary">{{ 'submit'|trans }}</button>
                </div>
            </div>
        </div>
    </main>
{% endblock %}
