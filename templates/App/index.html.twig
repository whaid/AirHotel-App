
{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid" id="img-accueil"></div>
    <div class="container">
        <h1>{{ 'index.home'|trans }}</h1>

        <table class="table">
            <thead>
            <tr>
                <th>Type</th>
                <th>{{ 'index.roomremaining'|trans }}</th>
                <th>{{ 'price'|trans }} *</th>
                <th>{{ 'see'|trans }}</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>{{ 'index.simple'|trans }}</td>
                <td>200</td>
                <td>60€</td>
                <td><a href="{{ path('room_list') }}">{{ 'index.showroom'|trans }}</a></td>
            </tr>
            <tr>
                <td>{{ 'index.double'|trans }}</td>
                <td>120</td>
                <td>70€</td>
                <td><a href="{{ path('room_list') }}">{{ 'index.showroom'|trans }}</a></td>
            </tr>
            <tr>
                <td>{{ 'index.family'|trans }}</td>
                <td>100</td>
                <td>85€</td>
                <td><a href="{{ path('room_list') }}">{{ 'index.showroom'|trans }}</a></td>
            </tr>
            </tbody>
        </table>
        * {{ "index.taxe"|trans }}
    </div>
{% endblock %}
